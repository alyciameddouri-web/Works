import { WorkExperience } from './work';

export default function App() {
  return (
    <main className="w-full h-full min-h-screen bg-black text-white">
      <WorkExperience baseRoute="/work" />
    </main>
  );
}
